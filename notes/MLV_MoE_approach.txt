Using SAM and Tesseract. Their perception improves when you constrain the hypothesis space.
Tesseract: yes
SAM: no
Need faster primitive shape detection.
Try OpenCV.

1. Architecture
Image → Vision Encoder → Visual Embeddings
Visual Embeddings → Projection Layer → LLM Token Space
LLM → Text Outputs

2. Start with a pretrained vision encoder + LLM
A. Vision encoder: CLIP ViT-B/32 (boring, stable, well-supported)
    Using the Hugging Face transformer (did not manage to use this on my NREL mac).
B. LLM: LLaMA 7B / 8B or Qwen 7B

3. Train the connector (projection layer or small transformer)
Option: LLaVA, BLIP-2, MiniGPT-4